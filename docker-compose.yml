services:
  gi:
    build: .
    container_name: gi-aion
    restart: unless-stopped
    ports:
      - "3010:3000"
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - DATABASE_URL=${DATABASE_URL}
